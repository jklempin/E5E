declare function enableCache(): void;
interface ipInfo {
    range: [number, number];
    country: string;
    region: string;
    eu: "0" | "1";
    timezone: string;
    city: string;
    ll: [number, number];
    metro: number;
    area: number;
}
declare function lookup4(stringifiedIp: string): Promise<ipInfo | null>;
declare const _default: {
    lookup: typeof lookup4;
    enableCache: typeof enableCache;
};
export = _default;
